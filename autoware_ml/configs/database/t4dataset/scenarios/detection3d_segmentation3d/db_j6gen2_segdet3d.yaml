_target_: autoware_ml.databases.t4dataset.t4scenarios.T4Scenarios
_convert_: all

# Root path where the scenario yaml files are stored
scenario_root_path: ${working_dir}/external/perception-devops/dataset_annotation/t4dataset/detection3d_segmentation3d/

# List of dataset parameters, and dataset_name is the yaml file name in T4Dataset,
# which consists of train/val/test splits
dataset_params:
  - _target_: autoware_ml.databases.scenarios.DatasetParams
    _convert_: all
    dataset_name: semseg_batch1_fixed_v1
    max_past_sweeps: 2
    max_future_sweeps: 0
    sample_steps: 1
    lidar_pointcloud_num_features: 7
    semantic_masks: true
  - _target_: autoware_ml.databases.scenarios.DatasetParams
    _convert_: all
    dataset_name: semseg_batch3_fixed_v1
    max_past_sweeps: 2
    max_future_sweeps: 0
    sample_steps: 1
    lidar_pointcloud_num_features: 7
    semantic_masks: true
