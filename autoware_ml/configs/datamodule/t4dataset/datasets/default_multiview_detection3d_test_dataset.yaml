_target_: autoware_ml.datamodule.t4dataset.dataset.T4Dataset
_convert_: all
# The datamodule calls this factory once per dataset source of the split, with the root
# path, the records and the supervision of that source.
_partial_: true

defaults:
  - detection3d@dataset_tasks.Detection3D: default_detection3d
  - /datamodule/transforms@transforms: default_multiview_detection3d_test_transforms

# When number of 3D GT bboxes in a scenario exceeds this number, we will select the first
# `max_num_3d_gt_bboxes` bboxes and ignore the rest. 500 is usually a
# common number of bboxes to include all 3D GT bboxes in a scenario.
max_num_3d_gt_bboxes: 500
